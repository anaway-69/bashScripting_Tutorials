#!/bin/bash
# SED:=
# --sed is a Unix utility that parses and transforms text, using a simple, compact programming language.


cat sedfile.txt | sed 's/H/LO/' > afterchangesedfile.txt


# where:-
# s is used to git checkout .
# g is used to change all words